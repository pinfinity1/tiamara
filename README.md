💄 Tiamara | تیاماراتیامارا یک پلتفرم تجارت الکترونیک مدرن و جامع برای فروش محصولات آرایشی و بهداشتی است که با تمرکز بر تجربه کاربری، هوش مصنوعی و شخصی‌سازی محصولات توسعه یافته است.این پروژه به صورت Full-Stack (Monorepo) طراحی شده و شامل بخش کلاینت (Next.js) و سرور (Express/Node.js) می‌باشد.✨ ویژگی‌های کلیدی🛍️ تجربه خرید و کاربرپروفایل پوستی هوشمند: سیستم تشخیص نوع پوست و نیازهای کاربر با ارائه پرسشنامه تخصصی ۱۰ مرحله‌ای و پیشنهاد محصولات متناسب با نوع پوست.دستیار هوشمند (AI Chat): چت‌بات اختصاصی "تیام" (قدرت گرفته از OpenAI) برای مشاوره خرید و پاسخگویی به سوالات.جستجوی پیشرفته: قابلیت جستجو در محصولات، برندها و دسته‌بندی‌ها با ذخیره تاریخچه جستجو.ویدیو شوکیس: نمایش ویدیوهای کوتاه معرفی محصول (مشابه استوری/ریلز) در صفحه اصلی.سبد خرید و پرداخت: پروسه خرید کامل، مدیریت سبد خرید (سینک شدن سبد مهمان و کاربر) و درگاه پرداخت زرین‌پال.🔐 امنیت و احراز هویتورود امن: سیستم ورود دو مرحله‌ای (رمز عبور + OTP) با استفاده از سرویس SMS.ir.محافظت امنیتی: استفاده از Arcjet برای جلوگیری از حملات بات، تشخیص ایمیل‌های فیک و Rate Limiting.مدیریت نشست: استفاده از NextAuth v5 و JWT با سیستم Refresh Token برای مدیریت سشن‌های امن.🛠️ پنل مدیریت پیشرفته (Super Admin)داشبورد آماری: گزارش درآمد، سفارشات اخیر، تعداد مشتریان و محصولات کم‌موجودی.مدیریت کاتالوگ: مدیریت کامل محصولات (با جزئیات تخصصی مثل ترکیبات و نوع پوست)، دسته‌بندی‌ها و برندها.مدیریت انبار: سیستم انبارداری با قابلیت ثبت تامین‌کنندگان، سفارش خرید (Purchase Order) و تاریخچه تغییرات موجودی.مدیریت محتوا: مدیریت بنرهای تبلیغاتی، اسلایدرها و چیدمان صفحه اصلی (Drag & Drop).مدیریت بازاریابی: ساخت و مدیریت کدهای تخفیف و روش‌های ارسال.🚀 تکنولوژی‌های استفاده شدهFrontend (Client)Framework: Next.js 15 (App Router)Language: TypeScriptStyling: Tailwind CSS, Shadcn UI, Lucide IconsState Management: ZustandForms: React Hook Form, ZodAuth: NextAuth.js v5 (Beta)UI Components: Embla Carousel, RechartsBackend (Server)Runtime: Node.jsFramework: Express.jsDatabase: PostgreSQLORM: PrismaServices:Cloudinary: مدیریت و آپلود تصاویر و ویدیوها.OpenAI: هوش مصنوعی چت‌بات.SMS.ir: سرویس ارسال پیامک OTP.Arcjet: لایه امنیتی برنامه.Infrastructure & DevOpsContainerization: Docker & Docker Compose.Web Server: Nginx (Reverse Proxy).CI/CD: GitHub Actions (برای بیلد و دیپلوی خودکار).🛠️ نصب و راه اندازیپیش‌نیازهاDocker و Docker ComposeNode.js (نسخه ۱۸ به بالا - در صورت اجرای دستی بدون داکر)روش اول: اجرا با Docker (توصیه شده)کلون کردن مخزن:git clone [https://github.com/your-username/tiamara.git](https://github.com/your-username/tiamara.git)
cd tiamara
تنظیم متغیرهای محیطی:یک فایل .env در ریشه پروژه بسازید (لیست متغیرها در پایین آمده است).اجرای پروژه در محیط توسعه:این دستور دیتابیس، سرور و کلاینت را بیلد و اجرا می‌کند.docker-compose -f docker-compose.dev.yml up --build
Client: http://localhost:3000Server API: http://localhost:5001PostgreSQL: در دسترس روی پورت 5436روش دوم: اجرای دستی (Local Development)1. تنظیم دیتابیس:یک دیتابیس PostgreSQL روی سیستم خود داشته باشید و DATABASE_URL را در فایل .env تنظیم کنید.2. راه‌اندازی سرور:cd server
npm install
npx prisma generate # تولید کلاینت پریزما
npx prisma migrate dev # اجرای مایگریشن‌ها و اعمال تغییرات دیتابیس
npm run seed # (اختیاری) ایجاد داده‌های اولیه مثل کاربر ادمین
npm run dev 3. راه‌اندازی کلاینت:در یک ترمینال جدید:cd client
npm install
npm run dev
🔑 متغیرهای محیطی (.env)یک فایل .env در ریشه پروژه بسازید و مقادیر زیر را تنظیم کنید:# --- Database Configuration ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=tiamara_db

# Connection String for Docker (Internal Network)

DATABASE_URL="postgresql://postgres:your_secure_password@postgres:5432/tiamara_db"

# Connection String for Local Dev (External Access)

# DATABASE_URL="postgresql://postgres:your_secure_password@localhost:5436/tiamara_db"

# --- Server Configuration ---

PORT=5001
CLIENT_URL=http://localhost:3000
API_BASE_URL_SERVER=http://server:3001/api # آدرس داخلی شبکه داکر

# --- Client Configuration ---

NEXT_PUBLIC_API_BASE_URL=http://localhost:5001/api
AUTH_SECRET=your_generated_secret_key # تولید با: openssl rand -base64 32

# --- Authentication (JWT) ---

JWT_SECRET=your_jwt_secret_key

# --- External Services ---

# Cloudinary (مدیریت فایل‌ها)

CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# OpenAI (برای چت‌بات هوشمند)

OPENAI_API_KEY=your_openai_key

# SMS.ir (برای ارسال پیامک OTP)

SMS_IR_API_KEY=your_sms_ir_api_key
SMS_IR_TEMPLATE_ID=your_template_id

# Arcjet (امنیت و Rate Limiting)

ARCJET_KEY=your_arcjet_key
📂 ساختار کلی پروژهtiamara/
├── client/ # کدهای فرانت‌اند (Next.js)
│ ├── src/
│ │ ├── app/ # صفحات و روت‌های برنامه (App Router)
│ │ ├── components/ # کامپوننت‌های UI، چت، محصولات و...
│ │ ├── store/ # مدیریت وضعیت با Zustand
│ │ ├── lib/ # ابزارها و کانفیگ‌ها
│ │ └── ...
├── server/ # کدهای بک‌اند (Express)
│ ├── src/
│ │ ├── controllers/ # منطق بیزنس و کنترلرها
│ │ ├── routes/ # تعریف مسیرهای API
│ │ ├── middleware/ # میدل‌ورهای احراز هویت و آپلود
│ │ └── ...
│ ├── prisma/ # مدل‌های دیتابیس (Schema) و مایگریشن‌ها
├── nginx/ # تنظیمات سرور Nginx برای پروداکشن
└── docker-compose.\*.yml # فایل‌های پیکربندی داکر
📄 لایسنساین پروژه تحت لایسنس MIT منتشر شده است. استفاده از آن برای مقاصد شخصی و تجاری آزاد است.
